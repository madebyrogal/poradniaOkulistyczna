{% extends 'base.html.twig' %}

{% block body %}
    <h1>Advertice list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Picture</th>
                <th>Alt</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for advertice in advertices %}
            <tr>
                <td><a href="{{ path('advertice_show', { 'id': advertice.id }) }}">{{ advertice.id }}</a></td>
                <td>{{ advertice.picture }}</td>
                <td>{{ advertice.alt }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('advertice_show', { 'id': advertice.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('advertice_edit', { 'id': advertice.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('advertice_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
